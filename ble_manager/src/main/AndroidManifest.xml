<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 基础蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 12+ 新增的运行时权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <!-- 声明BLUETOOTH_SCAN权限时，如果不需要获取位置，可以加上neverForLocation，这样就不需要定位权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" />

    <!-- 蓝牙特性声明 -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application>
        <!-- 前台服务 -->
        <service
            android:name=".ble_tradition.service.BlueClientService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".ble_tradition.service.BlueServerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity android:name=".ble_tradition.activity.SelectBlueHostOrGuest"
            android:theme="@style/AppTheme"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="colorMode|screenLayout|screenSize|uiMode|fontScale"
            />
        <activity android:name=".ble_tradition.activity.BlueClientActivity"
            android:theme="@style/AppTheme"
            android:screenOrientation="portrait"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustNothing|stateHidden"
            android:configChanges="colorMode|screenLayout|screenSize|uiMode|fontScale"
            />
        <activity android:name=".ble_tradition.activity.BlueServerActivity"
            android:theme="@style/AppTheme"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustNothing|stateHidden"
            android:configChanges="colorMode|screenLayout|screenSize|uiMode|fontScale"
            />
    </application>

</manifest>