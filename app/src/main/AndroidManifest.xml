<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 获取读写外置存储权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="AllFilesAccessPolicy,ScopedStorage" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:name="com.jhkj.videoplayer.app.MainApplication"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        tools:targetApi="33" >


        <activity
            android:name=".app.MainActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            tools:ignore="LockedOrientationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".pages.SelectConnTypeActivity"
            android:theme="@style/ScaleCenterTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            tools:ignore="LockedOrientationActivity"/>

        <activity
            android:name=".pages.ConnectionEditActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            android:windowSoftInputMode="adjustPan|stateHidden"
            tools:ignore="LockedOrientationActivity"/>

        <activity
            android:name=".pages.RemoteFilesActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            android:windowSoftInputMode="adjustPan|stateHidden"
            tools:ignore="LockedOrientationActivity"/>

        <activity
            android:name=".third_file_framework.SelectFileServerType"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            android:windowSoftInputMode="adjustPan|stateHidden"
            tools:ignore="LockedOrientationActivity"/>


        <activity
            android:name=".third_file_framework.smb_server.SmbServerActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:exported="true"
            android:launchMode="singleTop"/>
        <activity
            android:name=".third_file_framework.ftp_server.FtpServerActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            android:windowSoftInputMode="adjustPan|stateHidden"
            android:exported="true"
            android:launchMode="singleTop"/>


        <activity
            android:name=".third_file_framework.smb_server.IntentReceiverActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:relinquishTaskIdentity="true"
            android:taskAffinity=""
            android:exported="true"
            android:excludeFromRecents="true" />

        <service
            android:name=".third_file_framework.smb_server.service.SmbService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />
        <!-- 前台服务 -->
        <service
            android:name=".third_file_framework.ftp_server.FsService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <activity
            android:name=".player.VideoPlayerActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize|layoutDirection|locale|uiMode|colorMode"
            tools:ignore="LockedOrientationActivity"
            android:exported="true">

            <!--注册接收分享-->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <!--接收分享的文件类型-->
                <data android:mimeType="video/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="video/*" /><!-- 视频 -->
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/files_path" />
        </provider>
    </application>

</manifest>