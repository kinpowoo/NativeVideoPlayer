<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="SlideLeftRight" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <style name="SlideTopBottom" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_bottom</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_top</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_top</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_bottom</item>
    </style>

    <style name="Animation.NoAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/no_animation</item>
        <item name="android:activityOpenExitAnimation">@anim/no_animation</item>
        <item name="android:activityCloseEnterAnimation">@anim/no_animation</item>
        <item name="android:activityCloseExitAnimation">@anim/no_animation</item>
    </style>

    <style name="ScaleCenter" parent="@android:style/Animation.Activity">
        <!-- 新页面进入的动画（A->B时，B进入）-->
        <item name="android:activityOpenEnterAnimation">@anim/scale_in_center</item>
        <!-- 旧页面退出的动画（A->B时，A退出） -->
        <item name="android:activityOpenExitAnimation">@anim/no_animation</item>
        <!-- 旧页面重新进入的动画（B->A时，A返回进入） -->
        <item name="android:activityCloseEnterAnimation">@anim/no_animation</item>
        <!-- 新页面退出的动画（B->A时，B退出） -->
        <item name="android:activityCloseExitAnimation">@anim/scale_out_center</item>
    </style>

    <style name="AlertDialogStyle" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimAmount">0.5</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>